# GenAI Chat Assistant using RAG

## Project Overview

This project is a GenAI-powered chat assistant built using a Retrieval-Augmented Generation (RAG) architecture.
The assistant answers user queries by retrieving relevant information from a document knowledge base and using that information to generate grounded responses.

The goal of this project is to demonstrate how RAG systems combine document retrieval with AI-generated responses to reduce hallucinations and improve answer accuracy.

---

## Architecture

The system follows a Retrieval-Augmented Generation workflow:

1. Documents are stored in a knowledge base (`docs.json`)
2. Each document is processed and represented as embeddings
3. The user query is converted into an embedding
4. Similarity search retrieves the most relevant document chunks
5. Retrieved context is used to generate the response returned to the user

This ensures responses are grounded in the provided documents rather than purely generated by the model.

---

## Tech Stack

Backend:

* Python
* Flask

Frontend:

* HTML
* CSS
* JavaScript

Libraries Used:

* NumPy
* Scikit-learn

---

## Project Structure

gen-ai-assignment
│
├── app.py
├── docs.json
├── requirements.txt
├── README.md
│
├── static
│   ├── styles.css
│   └── script.js
│
└── templates
└── index.html

---

## Features

* Chat interface for user interaction
* Document-based knowledge system
* Retrieval-Augmented Generation pipeline
* Similarity-based document retrieval
* REST API for chat interaction
* Context-based responses from stored documents

---

## API Endpoint

POST `/api/chat`

Example Request:

{
"sessionId": "abc123",
"message": "How can I reset my password?"
}

Example Response:

{
"reply": "Users can reset their password from Settings > Security.",
"retrievedChunks": 1,
"tokensUsed": 0
}

---

## How to Run the Project

1. Clone the repository

git clone https://github.com/AravindMarapareddy/gen-ai-assignment.git

2. Navigate to the project folder

cd gen-ai-assignment

3. Install dependencies

pip install -r requirements.txt

4. Run the application

python app.py

5. Open the browser and go to

http://localhost:5000

---

## Example Query

User:
How can I reset my password?

Assistant:
Users can reset their password from Settings > Security.

---

## Future Improvements

* Use real embedding APIs
* Integrate LLM APIs such as OpenAI or Gemini
* Add a vector database for scalable document retrieval
* Improve conversation memory handling
* Deploy the application to a cloud platform

---

## Author

This project was developed as part of a GenAI assignment demonstrating the use of Retrieval-Augmented Generation for building AI-powered assistants.
